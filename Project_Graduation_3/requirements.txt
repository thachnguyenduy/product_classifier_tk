# Coca-Cola Sorting System - Python Dependencies (CONTINUOUS MODE)

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# NCNN (for fast inference on Raspberry Pi)
# Install with: pip install ncnn
# Or build from source: https://github.com/Tencent/ncnn
ncnn>=1.0.0

# Serial Communication
pyserial>=3.5

# GUI (included with Python)
# tkinter (usually pre-installed)

# Optional: For model conversion
# ultralytics  # If you need to convert YOLO to NCNN
